#!/usr/bin/env bash
# Script that changes the IP

# Change the IP address of the loopback interface to 127.0.0.2
sudo ifconfig lo 127.0.0.2

# Append '8.8.8.8 facebook.com' to /etc/hosts
sudo sh -c 'echo "8.8.8.8 facebook.com" >> /etc/hosts'