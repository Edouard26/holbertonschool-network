#!/usr/bin/env bash
#Script that change the IP

sudo cp /etc/hosts /etc/hosts.bak
read -p "127.0.0.2: " new_localhost_ip
read -p "8.8.8.8: " new_facebook_ip
{
    echo "$127.0.0.2       localhost"
    echo "$8.8.8.8         facebook.com"
} | sudo tee /etc/hosts > /dev/null